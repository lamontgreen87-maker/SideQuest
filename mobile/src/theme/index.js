import { colors, gradients } from './colors';
import { typography, fonts } from './typography';
import { spacing, layout } from './layout';

export const theme = {
    colors,
    gradients,
    typography,
    fonts,
    spacing,
    layout,
};

export { colors, gradients, typography, fonts, spacing, layout };
